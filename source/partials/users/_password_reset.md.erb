> POST: <%= @root_url %>/users/reset_password

```conf
// Coming soon
```

```javascript
// Coming soon
```

> Success - 1st check:

```yaml
{
  "message": "Confirmed, please resend with new password."
}
```

> Success - 2nd check:

```yaml
{
  "message":  "Password reset, please login."
}
```

<%= partial "partials/header_tokens.md.erb", :locals => {header_types: ["apikey"]} %>

- **Required fields:**
  - **1st check:** _(before the User enters a new password, you can (**preferred**) to verify the info is correct/hasn't expired)_
      - reset_key

  - **2nd check:** _(this actually resets the password)_
      - reset_key
      - new_password
      - new_password_confirmation

**NOTES:**

- Hitting this endpoint without the password fields will verify if the `reset_key` is valid, **please do this first to confirm code hasn't expired, default is 15 minutes**.

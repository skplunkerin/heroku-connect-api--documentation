> GET: <%= @root_url %>/documents

```conf
// Coming soon
```

```javascript
// Coming soon
```

> Success:

```yaml
<%= JSON.pretty_generate({
  "jwt":  @jwt_text,
  "documents":   data.documents.list
}).gsub('\\"', '') %>
```

<%= partial "partials/header_tokens.md.erb", :locals => {header_types: ["jwt"]} %>

